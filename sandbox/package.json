{
  "name": "pi-ds-sandbox",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "use:local": "npm install --no-save ../",
    "use:tgz": "echo '請先在上層執行 npm pack，然後 npm i ../yourteam-design-system-*.tgz'"
  },
  "dependencies": {
    "@yourteam/design-system": "file:.."
  },
  "devDependencies": {
    "sass": "^1.77.0",
    "vite": "^5.4.0"
  }
}
